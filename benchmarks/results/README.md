# Benchmark results

**Policy:** Only store raw, reproducible outputs. No marketing claims.

| Run | Date | Notes |
|-----|------|--------|
| *(none published yet)* | — | Run `python benchmarks/scripts/run_anla_ablation.py` and save JSON here |

### Naming

```
YYYY-MM-DD_anla_ablation.json
```

### Required fields

- `ANLA_OFF` / `ANLA_ON` counts from the script
- fixture version (`datasets/ablation_prompts.json`)
- git commit SHA

False-pass / false-block numbers without a fixed fixture and commit hash are not accepted as results.
